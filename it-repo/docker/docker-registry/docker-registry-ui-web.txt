# konradkleine/docker-registry:
docker run   -d   -e ENV_DOCKER_REGISTRY_HOST=192.168.11.20   -e ENV_DOCKER_REGISTRY_PORT=5000   -p 8080:80   konradkleine/docker-registry-frontend:v2

# можно поставить HARBOR
